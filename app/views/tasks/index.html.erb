<h1>My tasks</h1>
<%= link_to "Create a task", new_task_path %>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
    <% if task.completed %>
      <p>✔️</p>
    <% else %>
      <p>⬜</p>
    <% end %>
  <% end %>
</ul>
